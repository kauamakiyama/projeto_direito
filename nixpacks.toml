[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r jurisview/requirements.txt gunicorn"]

[start]
cmd = "gunicorn --chdir jurisview/backend app:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
